package handlers

import (
    "github.com/gorilla/mux"
)

func SetupRoutes(r *mux.Router) {
    // Define your routes here, for example:
    // r.HandleFunc("/api/playlists", GetPlaylists).Methods("GET")
    // Add more routes as needed
}
